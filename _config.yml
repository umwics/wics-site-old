baseurl: "/~wics"
sitename: "UM WICS"
locale: "en-US"
permalink: pretty

# Generates a sitemap.xml file
plugins:
    - jekyll-sitemap

# Used by jekyll-assets
assets:
    compression: true # Uglify files
    gzip: true
    caching:
        enabled: false
    sources:
        - _assets/

collections:
    pages: # Keep the root clean organizing all pages into _pages
        output: true
        permalink: /:path/
    co-op-companies:
